import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    flex: 1,
    paddingHorizontal: 24,
    marginBottom: 110,
  },
  button: {
    marginTop: 20,
    width: "100%",
  },
});
